function csrfToken(){const value=document.cookie.split('; ').find(row=>row.startsWith('csrftoken='));return value?decodeURIComponent(value.split('=')[1]):''}
async function apiFetch(url,options={}){const opts={credentials:'same-origin',headers:{'Accept':'application/json',...(options.body?{'Content-Type':'application/json'}:{}),...(options.headers||{})},...options};if(['POST','PUT','PATCH','DELETE'].includes((opts.method||'GET').toUpperCase()))opts.headers['X-CSRFToken']=csrfToken();const response=await fetch(url,opts);let data={};try{data=await response.json()}catch(e){}if(!response.ok){const message=data.detail||Object.values(data).flat().join(' ')||`Request failed (${response.status})`;throw new Error(message)}return data}
